<h1><%= @product.name %></h1>

<%= image_tag @product.image, :class => 'feature' if @product.image.presence %>
<ul>
    <li><%= @product.name %></li>
    <li>price $<%= @product.price %></li>
</ul>

    

<div class="controls">

<%= link_to 'choose the product', edit_product_path(@product) %>
<%= button_to 'Back', product_path, :method => 'get' %>
</div>










  
