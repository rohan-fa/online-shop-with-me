<h1><%= @product.name %></h1>



<%= image_tag @product.image, :class => 'feature' if @product.image.present> %>
<ul>
    <li><%= @product.name %></li>
    <li>price $<%= @product.price %></li>
</ul>

    <% @product.category.each do |category| %>
    <%= link_to image_tag(category.image, :class => 'thumb') %>
    <% end %>

<div class="controls">

<%= link_to 'Edit product', edit_product_path(@product) %>
<%= link_to 'Back', product_path %>
</div>




category_products_path





  
